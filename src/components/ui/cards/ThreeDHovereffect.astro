<div class="card-container">
    <div class="card">
        <div class="card__background">
            <img src="/src/assets/Images/no-avatar.png" alt="Background" />
        </div>
        <div class="card__content">
            <h3>Card Title</h3>
            <p>Some descriptive text here.</p>
        </div>
    </div>
</div>

<style>
    .card-container {
        perspective: 1000px; /* Adjust for the desired depth */
        display: inline-block; /* Or block, flex, etc. depending on your layout */
    }

    .card {
        width: 300px;
        height: 200px;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        transform-style: preserve-3d; /* Essential for 3D transformations of children */
        transition: transform 0.3s ease-in-out; /* Optional: Smooth transition for the card itself */
    }

    .card:hover {
        transform: rotateX(5deg) rotateY(5deg) scale(1.05); /* Optional: Subtle card tilt on hover */
    }

    .card__background {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
    }

    .card__background img {
        width: 110%; /* Slightly larger to avoid empty edges during transform */
        height: 110%;
        object-fit: cover;
        transform: translateZ(20px); /* Move the background slightly forward */
        transition: transform 0.3s ease-in-out;
    }

    .card:hover .card__background img {
        transform: translateZ(
            0
        ); /* Move back to its original Z position on hover */
    }

    .card__content {
        position: relative; /* To stack above the background */
        padding: 20px;
        color: #fff; /* Adjust as needed */
        text-align: center;
    }
</style>
